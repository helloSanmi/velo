apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  # Expose through ingress only; do not publish frontend directly.
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
